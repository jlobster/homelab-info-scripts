# Homelab Information Collection Configuration
# Copy this file to .env and customize for your environment
# 
# Setup Instructions:
# 1. Copy this file: Copy-Item env.example .env
# 2. Edit .env with your specific homelab details (see examples below)
# 3. Deploy scripts to your systems (see CLAUDE.md for deployment instructions)
# 4. Run the collection script: .\collect-homelab-snapshots.ps1

# SSH Connection Settings
# SSH username for connecting to your QNAP systems
# Example: admin, your_username
HOMELAB_USERNAME=your_username

# Path where scripts are deployed on QNAP systems
# Default QNAP path format: /share/homes/[USERNAME]
# Replace 'your_username' with your actual QNAP user directory
HOMELAB_QNAP_SCRIPT_PATH=/share/homes/your_username

# Path where scripts are deployed on Pi-hole/Raspberry Pi
# Default Pi path format: /home/[USERNAME]
# Replace 'your_username' with your actual Pi user directory  
HOMELAB_PIHOLE_SCRIPT_PATH=/home/your_username

# Output Directory Settings
# Leave empty to use Documents\Homelab, or specify a custom base directory
# Windows example: C:\MyBackups\Homelab
# Linux example: /home/user/backups/homelab
HOMELAB_OUTPUT_BASE_DIR=

# QNAP Systems Configuration
# Format: Name1,IP1;Name2,IP2;Name3,IP3
# Example: MainNAS,192.168.1.10;BackupNAS,192.168.1.11;MediaServer,192.168.1.12
# Replace with your actual QNAP system names and IP addresses
HOMELAB_QNAP_SYSTEMS=MainNAS,192.168.1.10;BackupNAS,192.168.1.11

# Pi-hole Configuration
# IP address of your Pi-hole system (running Debian/Raspberry Pi OS)
# Example: 192.168.1.5
HOMELAB_PIHOLE_IP=192.168.1.5

# SSH username for Pi-hole system (may be different from QNAP username)
# Common examples: pi, admin, your_username
HOMELAB_PIHOLE_USER=pi

# Script Tasks Configuration
# Format: Label1,Script1,Suffix1;Label2,Script2,Suffix2
# This defines what scripts to run on QNAP systems and how to name output files
# Suffix is added to output filename (empty = no suffix, -docker = adds "-docker")
# Default tasks collect system info and Docker info - modify as needed
HOMELAB_QNAP_TASKS=System info,sysinfo-qnap.sh,;Docker info,dockerinfo-qnap.sh,-docker

# Pi-hole Task Configuration
# Script filename to run on Pi-hole system for system information
HOMELAB_PIHOLE_SCRIPT=sysinfo-rpi.sh

# Whether to use sudo when running Pi-hole script
# Usually required (true) for accessing system info and Pi-hole database
HOMELAB_PIHOLE_USE_SUDO=true

# UI Settings
# Automatically open output directory when collection completes
HOMELAB_OPEN_OUTPUT_DIR=true

# Display progress messages and status updates during collection
HOMELAB_SHOW_PROGRESS=true

# Advanced Configuration (uncomment and modify if needed)
# These are optional settings for advanced users

# Custom SSH connection options (e.g., custom port, specific key file)
# HOMELAB_SSH_OPTIONS=-p 2222 -i ~/.ssh/homelab_key

# Disable specific collection types by setting to false
# HOMELAB_COLLECT_QNAP=true
# HOMELAB_COLLECT_PIHOLE=true